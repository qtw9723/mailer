// server/routes/mailer.js  ← TEMPORARY STUB, will be replaced in Task 6
import { Router } from 'express'
const router = Router()
router.get('/', (_req, res) => res.json({ status: 'stub' }))
export default router
